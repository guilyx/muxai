[project]
name = "muxai"
version = "0.0.0"
description = "Python SDK for muxai"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
